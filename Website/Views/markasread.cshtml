@inherits Umbraco.Web.Mvc.UmbracoViewPage
@using ContentModels = Umbraco.Web.PublishedContentModels;
@using umbraco.cms.businesslogic.member
@using Umbraco.Core.Services;
@using System.Web
@using System.Web.Security
@using Umbraco.Core.Models;
@using System.Linq;
@using System.IO;
@using System.Net.Mail;
@{
	Layout = null;
	
	
	var currentUser = Members.GetCurrentMember();
var _mediaService = ApplicationContext.Current.Services.MediaService;

var _memberService = ApplicationContext.Current.Services.MemberService;

}
@{
if(!string.IsNullOrEmpty(Request.Form["mediaid"])){

var mediaid = Convert.ToInt32(Request.Form["mediaid"]);

var media = _mediaService.GetById(mediaid);

media.SetValue("isread", 1);

_mediaService.Save(media);

}

}